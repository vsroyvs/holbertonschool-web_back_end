#!/usr/bin/env python3
""" Module that implements a function
that return a concatenated string of two strings"""


def concat(a: str, b: str) -> str:
    """return a concatenated string of two strings"""
    return a + b
